{
  "name": "flowwallet-web",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "setup": "npm --prefix backend run db:init",
    "start": "node start-all.js"
  }
}
